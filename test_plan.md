1. Modify `packages/web/src/components/ui/Input.tsx` to generate a fallback ID with `useId()`, link the label with `htmlFor={id}`, add `aria-hidden="true"` to the required asterisk, add `id` to the input element, compute `errorId` and `hintId` (only when rendered), combine them for `aria-describedby`, and add these IDs to the respective error and hint `<p>` tags.
2. Verify the modifications to `packages/web/src/components/ui/Input.tsx` using `tail -n 25 packages/web/src/components/ui/Input.tsx` and checking `grep` output.
3. Modify `packages/web/src/components/ui/Textarea.tsx` to generate a fallback ID with `useId()`, link the label with `htmlFor={id}`, add `aria-hidden="true"` to the required asterisk, add `id` to the textarea element, compute `errorId` and `hintId` (only when rendered), combine them for `aria-describedby`, and add these IDs to the respective error and hint `<p>` tags.
4. Verify the modifications to `packages/web/src/components/ui/Textarea.tsx` using `tail -n 25 packages/web/src/components/ui/Textarea.tsx` and checking `grep` output.
5. Modify `packages/web/src/components/ui/Select.tsx` to generate a fallback ID with `useId()`, link the label with `htmlFor={id}`, add `aria-hidden="true"` to the required asterisk, add `id` to the select element, compute `errorId`, set it as `aria-describedby` (or undefined), and add this ID to the error `<p>` tag.
6. Verify the modifications to `packages/web/src/components/ui/Select.tsx` using `tail -n 25 packages/web/src/components/ui/Select.tsx` and checking `grep` output.
7. Run tests in `packages/web` using `pnpm --filter @vetsaas/web test` and check for type errors by running `pnpm --filter @vetsaas/web build`. Note: A pre-existing issue causes `next lint` to fail in `@vetsaas/web` without specific setup, so we will use `build` for TS verification instead.
8. Complete pre-commit steps to ensure proper testing, verification, review, and reflection are done.
9. Submit the change by explicitly instructing the creation of a PR with the required title format '🎨 Palette: [UX improvement]' and description structure (What, Why, Before/After, Accessibility).
