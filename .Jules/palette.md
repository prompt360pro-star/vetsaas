## 2024-07-20 - Form Accessibility Improvements\n**Learning:** Added dynamic IDs using `useId()` to link labels with `htmlFor`, implemented `aria-describedby` for errors/hints, and hid purely decorative icons and required asterisks from screen readers.\n**Action:** Remember to use `useId()` as a fallback for missing `id` props and `aria-describedby` to link error/hint texts dynamically in custom form components.
